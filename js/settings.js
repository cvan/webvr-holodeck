/* global self */
(function () {

  self.settings = {
    firebaseRootUrl: 'https://webvr.firebaseio.com/holodeck'
  };

  // Uploadcare expects globals, so that's that.
  self.UPLOADCARE_LOCALE = 'en';
  self.UPLOADCARE_TABS = 'file url gdrive dropbox flickr box';
  self.UPLOADCARE_PUBLIC_KEY = '69c69e4fa8db4d4e3d8d';

})();
